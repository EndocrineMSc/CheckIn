package com.endocrine.checkin.di

import org.koin.core.module.Module
import org.koin.dsl.module

/** Data-layer bindings (Room, DataStore, repositories). Filled by later steps. */
val dataModule: Module = module {
}
