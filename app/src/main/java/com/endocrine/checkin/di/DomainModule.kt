package com.endocrine.checkin.di

import org.koin.core.module.Module
import org.koin.dsl.module

/** Domain-layer bindings (use cases). Filled by later steps. */
val domainModule: Module = module {
}
